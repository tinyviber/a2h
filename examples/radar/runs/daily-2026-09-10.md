# Run note — 2026-09-10

Written for a human, not for the pipeline. The machine-readable version is in
`.a2h/runs/`.

## What happened

Ingest ran at 07:00 and took 41 seconds. One feed (`model-tracker`) timed out
and succeeded on retry; the community feed returned three items that failed
schema validation and were skipped. 34 items landed in `inbox/`.

Dedupe dropped 11 of them. All 11 were genuine duplicates — four reposts of the
same price sheet, five summaries of the same outage, two autogenerated
changelog diffs.

Clustering formed 6 groups. One scored below the confidence threshold and would
normally have been discarded; it was kept because it is the only low-confidence
sample this week and it exercises the reject path.

## What is waiting

Three candidates in `publish-candidates/`. The run is deliberately stopped
here.

My read, for what it is worth:

- `draft-01-lattice-pricing.md` — approve. The price change is real, the
  silence around it is the story, and no one else has picked it up.
- `draft-02-skiff-launch.md` — hold. The window has slipped twice.
- `draft-03-noise.md` — reject. It is a status page with extra steps.

## What I would change next run

`vendor-watch` has produced nothing but duplicates for three days. It should be
demoted to `low` tier, or dropped. The diff in `patches/` does the demotion.
